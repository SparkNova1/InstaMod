
$ apt update -y && apt upgrade -y
$ pkg install git python-pip
$ git clone https://github.com/spark_nova4/InstaModa
$ cd "InstaModa"
$ python -m pip install -r requirements.txt
$ python Run.py
$ cd "InstaModa"
$ python Run.py
